import React from 'react';
import { ButtonInterface } from '@/types/ui';

const Button: React.FC<ButtonInterface> = () => {
  return (
    <button className="bg-theme-400">
    </button>
  );
}

export default Button;
